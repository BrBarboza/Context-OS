---
description: "Compatibility Command pra `/ctxos:config mode` — continua funcionando sem prazo de remocao. Mode agora vive em `/ctxos:config`."
argument-hint: "manual|autonomous"
disable-model-invocation: true
---

Compatibility Command — executa exatamente `/ctxos:config mode $ARGUMENTS`. Comportamento completo em `commands/config.md`.
