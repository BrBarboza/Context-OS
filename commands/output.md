---
description: "Compatibility Command pra `/ctxos:config output` — continua funcionando sem prazo de remocao. Output agora vive em `/ctxos:config`."
argument-hint: "compact|verbose"
disable-model-invocation: true
---

Compatibility Command — executa exatamente `/ctxos:config output $ARGUMENTS`. Comportamento completo em `commands/config.md`.
