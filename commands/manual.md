---
description: "Compatibility Command pra `/ctxos:config mode manual` — controle total, agente para e espera confirmacao antes de implementar e antes de commitar."
disable-model-invocation: true
---

Compatibility Command — executa exatamente `/ctxos:config mode manual`. Comportamento completo em `commands/config.md`.
