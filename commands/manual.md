---
description: Atalho pra `/ctxos:mode manual` — controle total, agente para e espera confirmacao antes de implementar e antes de commitar.
disable-model-invocation: true
---

Atalho — executa exatamente `/ctxos:mode manual`. Comportamento completo em `commands/mode.md`.
