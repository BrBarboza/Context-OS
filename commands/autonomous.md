---
description: Atalho pra `/ctxos:mode autonomous` — usuario conversa em linguagem natural, agente orquestra locate->implementa->commit internamente sem pausar, exceto decisao arquitetural.
disable-model-invocation: true
---

Atalho — executa exatamente `/ctxos:mode autonomous`. Comportamento completo em `commands/mode.md`.
