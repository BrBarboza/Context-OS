---
description: "Compatibility Command pra `/ctxos:config mode autonomous` — usuario conversa em linguagem natural, agente orquestra locate->implementa->commit internamente sem pausar, exceto decisao arquitetural."
disable-model-invocation: true
---

Compatibility Command — executa exatamente `/ctxos:config mode autonomous`. Comportamento completo em `commands/config.md`.
